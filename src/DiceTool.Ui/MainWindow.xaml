<Window x:Class="Dice.Ui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dice.Ui"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:CalculaterViewmodel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="28"/>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ProgressBar  Grid.Column="1" Margin="3" Maximum="1" Minimum="0" Value="{Binding Percentage}"/>
        <ListBox  Grid.Column="1" Grid.Row="1" Grid.RowSpan="2" Margin="3" ItemsSource="{Binding Results}" HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Value}" Grid.Column="0" HorizontalAlignment="Left"/>
                        <TextBlock Text="{Binding Propability, StringFormat={}{0:0.00} %}" Grid.Column="1" HorizontalAlignment="Right"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <TextBox  Margin="3" Grid.RowSpan="2" Text="{Binding Code}" AcceptsReturn="True"/>
        <Button Content="Calculate" Grid.Row="2" Padding="10" Margin="3" Command="{Binding CalculateCommand}"/>
    </Grid>
</Window>
